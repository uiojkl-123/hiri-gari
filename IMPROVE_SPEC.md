# 🌸 ひらがり — 고도화 스펙 (IMPROVE_SPEC.md)

> 목표 한 줄: **"같은 시간에 더 많이 외우고, 외운 걸 더 안 까먹게."**
> 기존 정체성(End-to-end · 자가주도 · 레벨제 · 한 입 크기)은 그대로 두고, **학습 효율 엔진**을 앱의 중심으로 승격시키는 개선 문서.
> (원본 기획: `README`/기획문서 · 기술 상세: `SPEC.md` 참고)

---

## 0. 현재 상태 진단 (뭐가 부족한가)

지금 앱은 **좋은 콘텐츠 뷰어 + 자가 체크**에 가깝다. 학습은 되는데, 아래가 비어 있음:

| 문제 | 지금 | 왜 느린가 |
|------|------|-----------|
| 복습 타이밍을 사람이 정함 | "학습 완료 ✓" 한 번 찍으면 끝 | 까먹을 때쯤 다시 안 꺼내줌 → 결국 다 까먹음 |
| 인식(recognition)만 함 | 4지선다 · 뜻 가리기 | "보면 아는데 못 쓰는" 상태에서 못 벗어남 |
| 약점이 안 쌓임 | 틀려도 그냥 지나감 | 자꾸 틀리는 항목(leech)이 방치됨 |
| 콘텐츠가 고정 | 단어 525 / 문장 고정 | 다 하면 끝. 예문·변형이 없음 |

→ **개선의 4개 축**: ① SRS 엔진 ② 능동 입력 ③ 약점 자동 관리 ④ AI 콘텐츠 확장.

---

## 1. 개선 방향 요약 (한눈에)

```
[더 빨리]  ①  SRS 복습 엔진        ← 핵심. 이거 하나가 체감의 80%
           ②  능동 입력 모드        ← 고르기 → 직접 타이핑 (production)
[더 많이]  ③  AI 예문 · 콘텐츠 생성 ← 개인용이라 가능한 무한 확장
           ④  약점/leech 자동 노트  ← 틀린 것만 집중 반복
[덜 까먹]  ⑤  데일리 큐 + 목표/스트릭 ← "오늘 할 20장"을 앱이 정해줌
```

우선순위: **① → ④ → ⑤ → ② → ③** (뒤로 갈수록 나중에 붙여도 됨)

---

## 2. 축 ① — SRS 복습 엔진 (최우선, 앱의 심장)

### 개념
"학습 완료 ✓"라는 단일 상태를 버리고, **모든 학습 항목(단어·한자·문장·카나)에 기억 상태를 부여**한다. 앱이 "오늘 복습할 카드"를 자동으로 뽑아준다.

### 카드 상태 모델
```
new → learning → review → mastered
                    ↑____↓ (틀리면 강등)
```

- **new**: 아직 안 본 항목
- **learning**: 오늘 처음 배우는 중 (짧은 간격 반복: 1분 → 10분 → 하루)
- **review**: 간격이 벌어지는 중 (1일 → 3일 → 7일 → 16일 …)
- **mastered**: 간격이 충분히 길어짐(예: 60일+). 사실상 졸업, 가끔만 등장

### 알고리즘 선택
개인용이니 **처음엔 SM-2(Anki 기본형)로 시작**하고, 여유되면 **FSRS**로 교체 가능한 구조로 짜자.

- **SM-2 (권장 시작점)**: 항목마다 `ease factor`, `interval`, `repetitions` 3개만 저장하면 됨. 구현 반나절.
- **FSRS (나중에)**: `difficulty / stability / retrievability` 기반 메모리 모델. 더 정확하지만 파라미터 튜닝 필요. 인터페이스만 맞춰두면 나중에 갈아끼기 쉬움.

> 설계 팁: `scheduler.review(card, grade) → { nextInterval, nextDue, newState }` 형태의 **순수 함수 하나**로 격리. 알고리즘 교체가 이 함수 교체로 끝나게.

### 채점 → 간격 연결 (기존 자가채점 재활용 👍)
네 앱의 강점인 **작문 4단계 자가채점(완벽/좋아/조금아쉽/다시)**을 SRS 등급으로 그대로 매핑하면 세계관이 딱 맞음:

| 자가 채점 | SRS grade | 다음 간격 |
|-----------|-----------|-----------|
| 완벽 (초록) | Easy | 크게 늘림 (×2.5) |
| 좋아 | Good | 정상 증가 (×ease) |
| 조금 아쉽 | Hard | 살짝만 늘림 (×1.2) |
| 다시 (빨강) | Again | 리셋 → learning으로 강등 |

→ 단어·한자·회화 카드에도 이 4버튼을 붙이면, **전 모드가 하나의 SRS로 통일**된다. 뜻 가리기 → 떠올리기 → 4버튼 채점 → 자동 스케줄.

### 화면
- **홈에 "오늘의 복습 N개" 카드** 최상단 배치. 이게 새 메인 진입점.
- 큐가 비면 "오늘 복습 끝! 🎉 새 단어 배우러 가기".

---

## 3. 축 ④ — 약점 · leech 자동 노트 (SRS와 함께 붙임)

### leech 감지
같은 카드가 **`Again`(다시)을 N회(예: 4회) 이상** 맞으면 `leech` 태그 자동 부여.

- leech 항목은 **별도 "약점 노트" 뷰**에 모임 (로드맵 1번 자동화 버전).
- 처리 옵션: (a) 집중 반복 세션 (b) 잠시 보류(suspend) (c) AI에게 "이거 왜 안 외워지지?" → 연상법/유래 요청.

### 오답 노트 (자동)
- 시험·작문에서 틀린/‘다시’ 채점한 항목이 **자동으로 노트에 적재**.
- "약점만 모아 5문제 시험" 버튼 → 틀린 것만 집중 타격.

> 이게 "더 빨리"의 숨은 핵심임. 아는 건 덜 보고 **모르는 것에 시간을 몰아주는** 구조.

---

## 4. 축 ⑤ — 데일리 큐 + 목표 + 스트릭

### 데일리 학습 큐
앱이 오늘치를 자동 조합해서 던져줌:
```
오늘의 학습 = [복습 due 카드들] + [신규 N개(목표 기반)]
예) 복습 24장 + 새 단어 10장 = 34장
```
- **신규 개수 목표**를 사용자가 설정(예: 하루 새 단어 10개). 이게 "학습 속도" 다이얼.
- 복습이 밀리면 신규를 자동으로 줄여줌 → **복습 폭탄 방지** (Anki 초보 이탈 1위 원인 제거).

### 스트릭 & 대시보드 (로드맵 5번)
- "연속 12일 · 오늘 34/34 완료" 담백한 표시 (기존 톤 유지: 요란한 뱃지 X).
- 모드별 mastered 비율, 향후 due 예측 그래프(내일 복습 몇 장 올지).

---

## 5. 축 ② — 능동 입력 모드 (recognition → production)

"보면 아는데 못 쓴다"를 깨는 단계. 카드마다 **입력 난이도**를 고를 수 있게:

1. **고르기** (현재) — 4지선다
2. **타이핑** — 뜻 보고 히라가나/한자 **직접 입력**. IME 없이도 되게 **로마자→가나 자동 변환** 입력창 제공(`ka`→`か`).
3. **작문** (현재, 확장) — 문장 통째 쓰기

> 채점: 타이핑은 **정규화 비교**(공백·구두점·오쿠리가나 흔들림 허용) + 애매하면 자가채점 폴백. 100% 자동정답 강요 안 하는 앱 철학 유지.

---

## 6. 축 ③ — AI 콘텐츠 생성 (개인용의 최대 강점)

개인용이라 **API 키 부담 없이 네가 직접 붙일 수 있는** 게 가장 큰 무기. 고정 525단어의 한계를 없앤다.

### 6-1. 예문 자동 생성 (단어 → 문맥)
- 단어 카드에 **"이 단어로 예문 3개" 버튼**. 레벨에 맞는 난이도로 생성.
- 문맥 속에서 외우면 암기 속도·유지력이 크게 오름(고립 단어 암기의 약점 보완).

### 6-2. "이것도 정답" 자동 확장 (작문)
- 지금은 수동으로 넣는 대체 표현을, 작문 채점 시 **AI가 자연스러운 다른 답 2~3개 + 뉘앙스 차이**를 즉석 생성.
- 자가채점 전 "내 답이 통하는지" AI에게 한 줄 피드백 받기(선택).

### 6-3. 오답 선택지(distractor) 자동 생성
- 시험 4지선다 오답을 **헷갈리기 좋은 것으로** AI가 생성(비슷한 발음/의미). 손으로 오답 짜는 노가다 제거.

### 6-4. 연상·유래 도우미 (leech 전용)
- 안 외워지는 한자: 부수 분해 + 한국 한자음 연결고리 + 짧은 스토리 생성.

### 구현 메모
- 실시간 호출 대신 **생성 결과를 로컬 캐시**(같은 단어 재생성 방지, 오프라인 대비).
- 응답은 **JSON 강제**(예문/읽기/뜻 구조화)해서 UI에 바로 매핑.
- 개인용이면 로컬 모델(Ollama 등)로도 예문 생성 충분 — 프라이버시 + 무료.

---

## 7. 콘텐츠 확장 (더 많이, 수동+반자동)

- **단어 525 → 목표 상향**: JLPT 빈도순/코어 리스트 기반으로 레벨별 증설. "많이"의 물리적 토대.
- **동사/형용사 활용 모드 추가**: ます형·て형·た형·부정형 드릴 (일본어 벽의 실질 구간). 활용은 규칙적이라 **자동 생성 카드**로 만들기 좋음.
- **카나 → 필기(획순) 옵션**(로드맵과 별개, 여유되면): 쓰기까지 가면 인식이 확 굳음.

---

## 8. 우선순위 & 단계별 도입 (개인용 현실 버전)

너 스타일(높은 에너지 시작 + 실용)에 맞춰 **작게 끊어서** 제안:

**Phase 1 — 엔진 이식 (제일 임팩트)**
- [ ] 카드 상태 모델(new/learning/review/mastered) + SM-2 스케줄러 순수함수
- [ ] 4버튼 채점 → grade 매핑 (기존 자가채점 재활용)
- [ ] 홈 "오늘의 복습 N개" 진입점

**Phase 2 — 안 까먹게**
- [ ] 데일리 큐(복습 due + 신규 목표), 복습 밀리면 신규 자동 감소
- [ ] leech 감지 + 약점 노트(오답 자동 적재)
- [ ] 스트릭/간단 대시보드

**Phase 3 — 더 깊게**
- [ ] 타이핑 입력 모드(로마자→가나 변환) + 정규화 채점
- [ ] AI 예문 생성 + "이것도 정답" 확장 (로컬 캐시)
- [ ] 동사/형용사 활용 드릴

**Phase 4 — 넓히기**
- [ ] 단어/한자 콘텐츠 증설, AI distractor, FSRS 교체 검토

---

## 9. 데이터 모델 변경 힌트 (개인용 = 로컬 우선)

계정 동기화는 나중. 우선 **IndexedDB**(용량·구조화 유리)로 로컬 저장.

```ts
// 학습 항목별 SRS 상태 (단어/한자/문장/카나 공통)
interface CardProgress {
  id: string;              // "word_042", "kanji_水", "sentence_110" ...
  type: 'kana' | 'word' | 'kanji' | 'sentence' | 'writing';
  state: 'new' | 'learning' | 'review' | 'mastered';
  ease: number;            // SM-2 ease factor (기본 2.5)
  interval: number;        // 일 단위
  reps: number;
  lapses: number;          // Again 누적 → leech 판정
  due: number;             // timestamp
  isLeech: boolean;
  lastGrade?: 'again' | 'hard' | 'good' | 'easy';
}

interface DailySession {
  date: string;            // YYYY-MM-DD
  newGoal: number;         // 하루 신규 목표
  reviewed: number;
  learnedNew: number;
  streak: number;
}
```

> 스케줄러는 순수함수로 격리:
> `review(card, grade) => { interval, due, state, ease }`
> → SM-2 ⇄ FSRS 교체가 이 함수 하나 갈아끼기로 끝남.

**스택 참고**(원본 스택 유지 전제): 새로 붙이면 Next.js 16+ / Node 24·26+ 기준. 로컬 저장은 IndexedDB 래퍼(Dexie 등)면 충분.

---

## 10. 성공의 기준 업데이트

기존 지표 + 효율 지표 추가:

- **복습 due 소화율**: 오늘 뜬 복습을 실제로 끝내는가 (= 안 까먹는 구조가 도는가).
- **mastered 전환 속도**: new → mastered까지 평균 며칠? (짧아지면 "더 빨리"가 검증됨).
- **leech 해소율**: 약점 노트에 들어온 항목이 다시 review로 복귀하는가.
- **신규 학습 지속성**: 복습 부담에 눌려 신규가 0이 되지 않는가 (큐 밸런싱이 되는가).

> 궁극 목표는 그대로: **"오늘도 조금 늘었다"** — 다만 이제 그 '조금'을 **앱이 최적 타이밍에 꺼내주는** 것.